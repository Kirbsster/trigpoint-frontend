// draw.js
export function render(state, deps) {
    const { ctx, canvas } = deps;
    const { view } = state;

    // the old drawAll() body goes here
    // use state.points, state.bodies, state.measurements, etc.
}